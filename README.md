# C-11-Cross-Platform-Judge
A lightweight cross-platform C++ auto-judge tool implemented in pure C++11 with no dependencies, supporting automatic compilation, batch test case execution and line-by-line output comparison for self-testing C++ algorithms, contest debugging and small project unit testing.
